typedef struct linknode
{
	struct listnode *next;
	data
	
}Node;

Node *p;

p = malloc(struct(node));

Node *q
q = malloc(...);
q->next = p;
NULL: //end of list

/* Exam will contain question on GDB or Valgrind
*/
